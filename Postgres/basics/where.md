## ```WHERE```

- used to filter row returned from the ```SELECT``` statement

```
SELECT
  columnA,
  columnB,
  columnC
FROM
  myTableName
WHERE
  conditions;
```

comparison operators:
```
=
>
<
>=
<=
<> or != //not equal
AND // logical operator
OR // logical operator
```
